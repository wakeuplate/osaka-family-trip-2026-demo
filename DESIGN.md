# 大阪旅遊手冊 Demo — Design System

這份文件是整體網站的 UI／UX 設計規範。它記錄設計為什麼這樣做，以及未來製作另一趟旅程時應該延續哪些原則；實際的 CSS token 與元件樣式則由 [`styles.css`](styles.css) 實作。

## 1. 設計定位

### 一句話

這是一份「像家人親手整理的日系旅遊手冊」：溫暖、清楚、容易操作，讓不同年齡的同行者在出發前和旅途中都能快速找到需要的資訊。

### 視覺關鍵字

`warm Japanese travel` · `family handbook` · `mobile-first` · `airy cards` · `friendly utility`

整體不是旅行社後台，也不是商務 Dashboard。它應該有一點紙本手冊和家庭相簿的感覺，但仍然保留數位工具的清楚層級與可操作性。

### 使用者

- **旅行規劃者**：需要快速確認每日路線、住宿、交通與實用資訊。
- **同行長輩**：需要大字、明確標題、少干擾與容易理解的資訊層級。
- **小朋友與年輕家人**：透過照片、emoji、顏色與短句理解旅程。

## 2. 產品體驗原則

### 先回答「現在要去哪裡」

首頁與每日行程是主要入口。使用者不需要先理解複雜的分類，就能看到 Day 1–Day 5，接著展開景點與細節。

### 把複雜資料收起來，但不要藏掉

航班、房間、包車與緊急資訊保留在實用資訊頁，使用收合區塊降低日常瀏覽負擔。重要的標題、時間、地點與注意事項仍然要在第一眼可辨識。

### 為家庭共用，而不是只為資料擁有者設計

文字避免像訂單或旅行社文件一樣生硬。流程、時間與注意事項要清楚，但語氣保持像一位已經替大家整理好資料的家人。

### Mobile-first by intent

這個網站原本就是旅行中用手機查資料的工具，因此手機版不是桌面版的縮小版，而是主要產品。桌面瀏覽器只把手機畫布置中，不另外發展多欄 Dashboard、側邊欄或 hover 才能操作的介面。

## 3. 主網站的視覺系統

### 色彩

主網站使用「海濱晴空 Seaside Sky」配色：以暖紙色承接日系手冊感，以海藍建立導覽與資訊辨識，再用珊瑚橘放在需要行動的地方。

| Token | 色彩 | 用途 |
| --- | --- | --- |
| `--paper` | `#FBF7F0` 暖米白 | App 主背景，不使用冷白 |
| `--card` | `#FFFFFF` | 主要卡片表面 |
| `--sink` | `#F7F1E8` | 內嵌、輸入欄位與按壓表面 |
| `--primary` | `#3E8DAA` 海藍 | 導覽、連結、主要資訊標示 |
| `--primary-d` | `#2E6E88` 深海藍 | 文字型連結與較高對比狀態 |
| `--accent` | `#FF7E5C` 珊瑚橘 | CTA、重要行動與亮點 |
| `--soft` | `#E3F1F6` 淺藍 | 資訊 tag、柔和區塊背景 |
| `--sand` | `#F5E7D0` 沙色 | 旅遊提示、費用與次要提醒 |
| `--ink` | `#3A3330` 暖黑 | 主要文字，不使用純黑 |
| `--ink-2` | `#6B615B` 暖灰 | 說明文字與次要資訊 |
| `--line` | `#ECE3D8` 淡米線 | 分隔線與輸入框邊界，少量使用 |

### 色彩使用規則

- 大面積使用暖米白與白色卡片，讓內容有呼吸空間。
- 海藍負責「我現在在哪裡、這是什麼資訊」；珊瑚橘負責「下一步要做什麼」。
- 提示使用沙色或淡黃色，不用大面積鮮紅製造焦慮；真正的危險狀態才使用 `--danger`。
- Day Card 使用同一色系由淺到深的五段色階，讓五天有連續感，而不是五種互相競爭的顏色。
- 不使用紫色漸層、冷灰背景、純黑大面積區塊或過度霓虹色。

### 字體與文字層級

- **`Noto Sans TC`**：繁體中文主要字體，用於內文、標籤、按鈕與一般資訊。
- **`M PLUS Rounded 1c`**：數字、Day 編號、時間與主要標題，增加圓潤和親切感。
- **`Noto Serif TC`**：只用於行前互動影片的故事型大標題，讓影片和主網站有清楚但不衝突的分支。

文字層級以「大標題 → 卡片標題 → 重要數字／時間 → 說明文字 → 輔助標籤」排列。長輩需要看的內容不能只靠小字、低對比或顏色差異傳達。

### 形狀、卡片與陰影

- 一般卡片使用 `22px` 圓角，內嵌區塊使用 `16px`，按鈕與 chip 使用完整膠囊形。
- 卡片以白底和柔和暖色陰影建立層次，避免每個區塊都加硬邊框。
- 卡片內距約 `16–18px`，頁面左右 gutter 約 `16px`，保留足夠留白。
- 圖片卡片使用 `16:9` 橫幅比例；圖片上的文字需要保護漸層或足夠對比。
- 元件可以可愛，但不能讓陰影、圓角或裝飾搶過行程與時間資訊。

## 4. 版面與導覽

### 畫布

- 目標寬度：`390–430px`。
- App 最大寬度：`430px`，桌面瀏覽時置中顯示。
- 單欄、垂直滑動、拇指可操作；不依賴 hover。
- 底部導覽固定在畫面底部，內容區預留安全距離與 safe-area padding。
- 不另做桌面版資訊架構；桌面只作為手機版展示容器。

### 主要導覽

```text
首頁
├── 每日行程 → Day 1–Day 5 → 景點詳情 → 地圖搜尋
├── 實用資訊 → 航班／住宿／包車／緊急資訊
├── 匯率計算 → 固定 Demo 匯率與本機編輯
└── 常用日文 → 指給對方看與瀏覽器朗讀

首頁 → 行前互動影片 → 圖像化旅程導覽 → 返回旅遊手冊
```

首頁要同時扮演「入口」和「公開版說明」：Demo 資料聲明、規劃版提示與行前影片入口應該容易找到，但不能壓過每日行程本身。

## 5. 核心元件規範

| 元件 | 設計目的 | 規則 |
| --- | --- | --- |
| 首頁公告卡 | 先說明公開版資料邊界 | 使用淡色提示卡，文字短、可快速掃讀 |
| Day Card | 讓使用者先掌握每天主題 | 左側日期色塊，右側主題、住宿與活動摘要；點擊展開 |
| Timeline | 呈現單日詳細順序 | 時間、地點與動作對齊，避免一大段文章 |
| 景點卡 | 在行程脈絡中補充景點資訊 | 16:9 圖片、名稱、短介、費用／時間 tag、注意事項、地圖入口 |
| 實用資訊卡 | 集中高密度旅程資料 | 使用 definition rows、收合區塊與複製按鈕降低閱讀負擔 |
| Tag／Chip | 快速分類費用、時間與狀態 | 短字、少量使用；不把整段句子塞進 tag |
| CTA 按鈕 | 指示下一個動作 | 珊瑚橘、至少 `48px` 高、動詞清楚 |
| 底部導覽 | 在主要工具間快速切換 | 固定、單手可觸及；目前頁面用海藍或圖示狀態表示 |
| 常用日文卡 | 讓使用者直接展示或朗讀 | 中文說明、日文大字、假名／拼音與朗讀按鈕分層 |

## 6. 互動與動態

- Day Card、景點亮點、旅客／房間等資訊使用展開／收合，讓使用者依當下需要閱讀。
- 地圖入口、複製按鈕與語音朗讀都要有明確的按壓回饋。
- 主要按鈕最小高度為 `48px`；地圖圓形入口等次要圖示按鈕可使用 `40px`，但仍需保留清楚的間距。
- 頁面切換使用短淡入與微幅位移，按壓使用輕微縮放；避免長時間、持續或會干擾查資料的動畫。
- 行前影片可以使用較有情緒的 Ken Burns、場景淡入與自動播放，但必須支援點按跳段、按住快轉與 `prefers-reduced-motion`。
- 動態是用來說明旅程與回應操作，不是裝飾性背景效果。

## 7. 內容與語氣

- 以繁體中文為主，英文只保留必要的地名、代碼、品牌或技術名稱。
- 一行盡量只放一個意思；時間、日期、地點與注意事項要明確寫出。
- 語氣像替家人整理旅程的人：溫暖、直接、帶一點輕鬆感，不使用旅行社或企業公告口吻。
- emoji 是分類與快速辨識工具，不是裝飾灑滿畫面；同一類資訊應保持一致的 emoji。
- 先講對使用者有幫助的內容，再補充限制或注意事項。
- 公開版涉及資料時使用 `Demo`、`***` 或合成內容，並清楚標示「規劃版／展示版，非實際行程」。

## 8. 行前互動影片的視覺分支

行前影片與主網站共享「家庭旅行、中文優先、手機直向、容易理解」的產品方向，但採用更像短篇旅遊影片的視覺語言，不強行複製主網站的卡片 UI。

| 項目 | 規範 |
| --- | --- |
| 調性 | 日式夏日、溫暖、有故事感、像旅程預告 |
| 色彩 | 暖白紙色 `#FAF5EC`、朱紅 `#D7402B`、藍染 `#22406B`、金色 `#C9A227` |
| 字體 | `Noto Serif TC` 故事標題，`Noto Sans TC` 輔助資訊 |
| 紋理 | 和紙質感、青海波等日式紋樣，使用節制 |
| 動態 | 全螢幕分幕、淡入、照片 Ken Burns、進度條與點按換幕 |
| 互動 | 自動播放、點按上一幕／下一幕、按住快轉、結尾可重播或返回手冊 |
| 內容 | 大字幕、照片輪播、短句；每幕只傳達一個旅程重點 |

影片的任務是「讓家人先看懂旅程」，不是取代主網站的詳細查詢功能。

## 9. 可讀性與無障礙底線

- 重要內容不能只用顏色區分，需搭配文字、圖示或標題。
- 互動元件要有可理解的文字或 `aria-label`，尤其是圖示按鈕。
- 不把 hover 當成必要操作；觸控與鍵盤都應有可理解的狀態。
- 維持大字、足夠行距與短段落，避免為了塞更多內容而縮小文字。
- 尊重 `prefers-reduced-motion`，減少淡入、縮放與 Ken Burns 動畫。
- 外部地圖與官方連結要保持清楚的出口感，不讓使用者誤以為公開 Demo 會處理訂單或即時服務。

## 10. 公開版的資料與隱私設計

- UI 可以保留真實產品需要的資訊區塊，但內容必須使用去識別化或合成資料。
- 航班、旅客、訂單、房間、導遊、包車與財務資訊不可使用真實值。
- 可編輯欄位只存在目前瀏覽器的 `localStorage`，不建立後端資料流。
- 不加入 analytics、tracking、API key 或私人 Netlify／影片連結。
- 圖片公開前要移除 EXIF、comment 等 metadata，並確認素材授權記錄在 [`CREDITS.md`](CREDITS.md)。

## 11. 維護規則

| 要改的內容 | 應該修改 |
| --- | --- |
| 顏色、字體、圓角、陰影、版面 token | 先更新本文件，再同步 `styles.css` |
| 行程、景點、住宿與 Demo 欄位 | `demo-data.js`，不要把資料寫死在元件 markup |
| 導覽、收合、複製、匯率與朗讀行為 | `app.js` |
| 行前影片的場景、動畫與影片專屬色彩 | `行前影片/index.html`，並同步本文件第 8 節 |
| 公開版敘事與作品集說明 | `README.md`、`README_EN.md` |

修改視覺時，先問自己三件事：

1. 這個改動是否讓旅途中查資料更快？
2. 長輩或小朋友是否更容易理解？
3. 它是否仍然屬於溫暖、清楚、非商務的家庭旅遊手冊？

如果答案是否定的，應該先重新檢查需求，而不是只增加裝飾。
